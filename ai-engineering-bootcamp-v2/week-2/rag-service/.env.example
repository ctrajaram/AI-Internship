# Same OpenAI key as Week 1 — used for both chat completions and embeddings.
OPENAI_API_KEY=

# Week 2 — Pinecone vector store. Key from app.pinecone.io -> API Keys.
# The index is created by `python setup_index.py` (dim 1536, cosine).
PINECONE_API_KEY=
PINECONE_INDEX=northwind-rag
